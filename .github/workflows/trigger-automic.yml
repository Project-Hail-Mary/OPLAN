name: Test Workflow

on:
  push:
    paths:
      - '**/*.md'

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v2

      - name: Run a one-liner script
        run: echo "This is a test workflow running successfully!"
